This folder contains GitHub Actions workflows for building and deploying the site.
Keep a single Pages workflow to avoid duplicate runs. Active workflow: publish.yaml
If pages.yml reappears, delete it or disable it to prevent double builds.